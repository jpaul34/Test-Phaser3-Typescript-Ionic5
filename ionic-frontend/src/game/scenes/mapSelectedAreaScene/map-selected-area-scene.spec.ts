import { MapSelectedAreaScene } from './map-selected-area-scene';

describe('MapSelectedAreaScene', () => {
  it('should create an instance', () => {
    expect(new MapSelectedAreaScene()).toBeTruthy();
  });
});
